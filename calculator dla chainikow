#Создаём переменную в какую мы будем передавать действие (умножение, деление и т. д.)
what = input("Введите действие (*, /, +, -): ")

#Создаём 2 переменные в которые мы будем передават числа
#float мы используем в случае если мы в переменную будем передавать числа с плавающей точкой(например 1.5, 2.7 и т. д.), а int если мы будем передавать в переменную целые числа(например 1, 2, 3 и т.д.)
digit_1 = float(input("Введите 1 число: "))
digit_2 = float(input("Введите 2 число: "))

#Пишем код для вычисления
if what == "*": #Если what = * то мы умножаем digit_1 на digit_2
    answer = digit_1 * digit_2 #Тут я думаю всё и так понятно :)
    print(digit_1, "*", digit_2, "=", answer) #А тут мы просто выводим ответ (можно просто написать print(answer)

#Тут всё так же как и в строчках 9-12 только * мы меняем на /
elif what == "/":
    answer = digit_1 / digit_2
    print(digit_1, "/", digit_2, "=", answer)

#Тут всё так же как и в строчках 9-12 только * мы меняем на +
elif what == "+":
    answer = digit_1 + digit_2
    print(digit_1, "+", digit_2, "=", answer)

#Тут всё так же как и в строчках 9-12 только * мы меняем на -
elif what == "-":
    answer = digit_1 - digit_2
    print(digit_1, "-", digit_2, "=", answer)

#Если ни один с if не сработает то выведется "Вы допустили ошибку!"
else:
    print("Вы допустили ошибку!")
